package ua.denis.dev.bot.listeners;

import com.pengrad.telegrambot.TelegramBot;
import okhttp3.Request;

public class MessageHandler {
  private void sendMessage(TelegramBot bot) {
      bot.execute(new Request())
  }
}
